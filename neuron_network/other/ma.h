#ifndef MAIN_H
#define MAIN_H

#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <stdio.h>
#include <stdlib.h>

// Include custom headers
#include "../solver/solve.h"

// Function declarations
void pro_grid(); // Assuming `pro_grid` is defined somewhere else.
void pro_word(); // Assuming `pro_word` is defined somewhere else.
void pro_solv(); // This function should be defined in `solve.h` or elsewhere.

#endif // MAIN_H